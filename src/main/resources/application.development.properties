# App version
appVersion=1.0
# Spark static files
staticFileLocation=/public
# Websocket endpoint
webSocketLocation=/websockets
# HTTP Port
httpPort=3000
# Only set to false for development; Mustache doesn't disable caching by default - workaround
cacheTemplates=false
# Environment
environment=DEVELOPMENT
